<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MinicapApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MinicapApp" parent="Base.Theme.MinicapApp" />

    <!-- Main Activity Theme -->
    <style name="Theme.MainActivity" parent="Base.Theme.MinicapApp">
        <item name="colorPrimary">@color/main_primary</item>
        <item name="colorAccent">@color/main_accent</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <!-- Settings Activity Theme -->
    <style name="Theme.SettingsActivity" parent="Base.Theme.MinicapApp">
        <item name="colorPrimary">@color/settings_primary</item>
        <item name="colorAccent">@color/settings_accent</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <!-- Controller Activity Theme -->
    <style name="Theme.ControllerActivity" parent="Base.Theme.MinicapApp">
        <item name="colorPrimary">@color/controller_primary</item>
        <item name="colorAccent">@color/controller_accent</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <!-- Bluetooth Activity Theme -->
    <style name="Theme.BluetoothActivity" parent="Base.Theme.MinicapApp">
        <item name="colorPrimary">@color/bluetooth_primary</item>
        <item name="colorAccent">@color/bluetooth_accent</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <!-- Recorded Data and Data Item Activity Theme -->
    <style name="Theme.DataActivity" parent="Base.Theme.MinicapApp">
        <item name="colorPrimary">@color/data_primary</item>
        <item name="colorAccent">@color/data_accent</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <!-- Materials Information Page Activity Theme -->
    <style name="Theme.MaterialsInformationPageActivity" parent="Base.Theme.MinicapApp">
        <item name="colorPrimary">@color/materials_information_page_primary</item>
        <item name="colorAccent">@color/materials_information_page_accent</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

</resources>